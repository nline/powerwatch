sudo: false
language: node_js

addons:
  apt:
    packages:
      - expect

cache:
  directories:
    - "node_modules"
    - "$HOME/bin"

before_script:
  - true #npm install -g particle-cli
  - bash <( curl -sL https://particle.io/install-cli )
  - export PATH="$PATH:$HOME/bin"

script:
  - ./.particle-cli-login.expect "$particle_email" "$particle_password" "$(which particle)"
  - (particle compile electron firmware/particle/plugwatch_C --target 0.7.0)
  - (particle compile electron firmware/particle/plugwatch_F --target 0.7.0)
